persistence:
  pg_url: postgresql://vandelay_app:secret@postgres-test:5432/vandelay_test
integrations:
  vendors:
    one:
      api_base_url: vendor-one
    two:
      api_base_url: vendor-two
environment: test